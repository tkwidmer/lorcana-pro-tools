name: Tournament Tracker Tick

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch: {}

jobs:
  tick:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger tournament tracker tick
        run: |
          curl --fail-with-body -sS -X POST \
            "https://lorcana-pro-tools.vercel.app/api/discord-tournament-tick" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"
